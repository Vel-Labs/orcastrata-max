# Orcastrata Native Interface V1

## Objective

Make Orcastrata's existing workflows available as native `/orcastrata-*`
commands and load concise Parent/PM guidance through trusted Codex plugin hooks.

## Acceptance Oracle

A clean package installation in an isolated Codex home exposes every documented
`/orcastrata-*` command, loads the hook on a fresh session, preserves the
existing `$codexmax-orchestrator:*` skill interface, and passes focused package
validation. External adversarial reviewers can advise, but the Parent accepts
only repository-backed findings.

## Authority

The user approved planning, exact MiniMax, Grok, and DeepSeek Pro review calls,
source changes, tests, package staging, and isolated local installation. Do not
modify the dirty main checkout. Do not push or publish.

## Scope

Work only in:

`/Users/steven/Workspace/40_Code/_worktrees/orcastrata-dynamic-routing-v2`

## Design Rule

Commands are thin aliases over existing skills. Hooks add concise context only.
They do not create a second board, scheduler, or orchestration runtime.

## Stop Rule

Do not simulate a native command with prompt parsing. Do not weaken provider,
installation, hook-trust, or package checks to obtain a passing result.
